# DO NOT MODIFY: This is autogenerated.
# You can edit ".scripts/update-workflows"

name: Direct Test
"on":
  push:
    branches:
      - "**"
      - "!no-tests/**"
      - "!master"
      - "!1.1/development"
      - "!1.0.x/master"
      - "!1.0.x/development"
      - "!1.0.x/experiment"
    tags:
      - "!**"
  pull_request:
    branches:
      - "**"
      - "!no-tests/**"
jobs:
  direct-test:
    name: Test of Action-setup-swift
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macOS-latest
        swift-version:
          - 5.1.2
          - DEVELOPMENT-SNAPSHOT-2019-11-20-a
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: Run the smoke test.
        run: npm ci && npm run smoke-test
      - name: Install Swift
        env:
          INPUT_SWIFT-VERSION: ${{ matrix.swift-version }}
        run: node ./lib/main.js
      - name: View Swift Version
        run: swift --version
      - name: Run Swift
        run: ./.scripts/swift-test
  direct-test-with-swift-version-file:
    name: Test of Action-setup-swift
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macOS-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: Run the smoke test.
        run: npm ci && npm run smoke-test
      - name: Install Swift
        env:
          INPUT_SWIFT-PACKAGE-DIRECTORY: ./swift-test-package
        run: node ./lib/main.js
      - name: View Swift Version
        run: swift --version
      - name: Run Swift
        run: ./.scripts/swift-test
