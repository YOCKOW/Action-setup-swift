# DO NOT MODIFY: This is autogenerated.
# You can modify ".scripts/update-workflows"
name: Direct Test
on:
  push:
    branches:
      - '*'
      - '*/*'
      - '!master'
      - '!1.0.x/development'
      - '!1.0.x/experiment'
    tags:
      - '!*'
  pull_request:
    branches:
      - '*'
      - '*/*'
jobs:
  test:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macOS-latest
        swift-version:
          - '5.1.1'
          - 'DEVELOPMENT-SNAPSHOT-2019-10-21-a'
    runs-on: ${{ matrix.os }}
    name: Test of Action-setup-swift
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v1
      with:
        node-version: '12.x'
    - name: Run the smoke test.
      run: npm ci && npm run smoke-test
    - name: Install Swift.
      run: node ./lib/main.js
      env:
        INPUT_SWIFT-VERSION: ${{ matrix.swift-version }}
    - name: View Swift Version
      run: swift --version
    - name: Run Swift
      run: ./.scripts/swift-test
