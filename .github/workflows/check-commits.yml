# DO NOT MODIFY: This is autogenerated.
# You can edit "utils/update-workflows"

name: Check commits
"on":
  push:
    branches:
      - "**"
      - "!no-tests/**"
    tags:
      - "!**"
  pull_request:
    branches:
      - "**"
      - "!no-tests/**"
jobs:
  check-modules:
    name: Check whether or not unnecessary modules are installed.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - uses: actions/setup-python@v1.1.1
        with:
          python-version: "3.7"
      - name: Check modules.
        run: ./utils/check-modules
  check-traspilation:
    name: Check whether or not transpilation is completed.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - name: Check transpilation
        run: |+
          npm install
          npm run build
          if [ -n "$(git diff -- ./lib)" ]; then
            echo "Transpilation has not been completed."
            exit 1
          fi
