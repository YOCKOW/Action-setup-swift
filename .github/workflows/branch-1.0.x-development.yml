# DO NOT MODIFY: This is autogenerated.
# You can edit ".scripts/update-workflows"

name: Test for branch "1.0.x/development"
"on":
  push:
    branches:
      - 1.0.x/development
    tags:
      - "!*"
jobs:
  branch-test:
    name: Test of Action-setup-swift
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macOS-latest
        swift-version:
          - 5.1.1
          - DEVELOPMENT-SNAPSHOT-2019-10-21-a
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@master
      - name: Install Swift.
        uses: YOCKOW/Action-setup-swift@1.0.x/development
        with:
          swift-version: ${{ matrix.swift-version }}
      - name: View Swift Version
        run: swift --version
      - name: Run Swift
        run: ./.scripts/swift-test
